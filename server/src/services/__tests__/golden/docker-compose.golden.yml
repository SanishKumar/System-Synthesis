# Generated by System Synthesis
# IR schema: 1.0
# Target: docker-compose
# Source graph SHA-256: 74fa3b807cad6b893cfdcc80ece03664602997555680c6f4c0690380931ba1ac
# Deterministic output: timestamps are intentionally omitted

version: "3.9"

services:
  api:
    image: node:20-alpine
    container_name: api
    deploy:
      replicas: 2
    restart: unless-stopped
    ports:
      - "3000:3000"
    command: node server.js
    depends_on:
      - main-db
    networks:
      - private

  main-db:
    image: postgres:16.3-alpine
    container_name: main-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "${POSTGRES_DB:-appdb}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}"
      POSTGRES_USER: "${POSTGRES_USER:-app}"
    volumes:
      - main-db-pgdata:/var/lib/postgresql/data
    networks:
      - restricted

volumes:
  main-db-pgdata:
    driver: local

networks:
  private:
    driver: bridge
  restricted:
    driver: bridge
